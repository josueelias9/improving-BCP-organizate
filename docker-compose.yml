services:

  new-service:
    build: 
      context: ./new-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/workspace
    environment:
      - ENV=development
    command: sleep infinity
